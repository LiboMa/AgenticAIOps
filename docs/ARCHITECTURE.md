# AgenticAIOps MVP - 系统架构文档

**版本:** v2.0
**更新日期:** 2026-02-12
**维护者:** AgenticAIOps Team

---

## 1. 系统概述

AgenticAIOps 是一个基于 AI Agent 的云运维自动化平台，通过自然语言交互实现 AWS 云资源的监控、诊断和自动化运维。

### 1.1 核心能力

```
┌─────────────────────────────────────────────────────────────┐
│                    AgenticAIOps MVP                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  🔍 监控扫描      → 13 AWS 服务自动扫描                      │
│  🧠 智能诊断      → RCA + 异常检测                           │
│  📋 标准化运维    → SOP 系统 + 知识库                         │
│  🔄 闭环学习      → Pattern → S3 + OpenSearch → Action       │
│  💬 自然语言交互  → Chat + API + WebUI                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 技术架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                         Frontend (React + Ant Design)             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ AgentChat│ │ Dashboard│ │ScanConfig│ │ Settings │            │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘            │
│       └─────────────┴─────────────┴─────────────┘                │
│                          HTTP/REST API                            │
├──────────────────────────────────────────────────────────────────┤
│                    Backend (FastAPI + Uvicorn)                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────┐        │
│  │              api_server.py (~2800+ lines)             │        │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │        │
│  │  │ Chat Engine│  │ API Routes │  │ A2UI Engine│     │        │
│  │  └─────┬──────┘  └─────┬──────┘  └────────────┘     │        │
│  │        └───────────┬────┘                             │        │
│  └────────────────────┼──────────────────────────────────┘        │
│                       │                                           │
│  ┌────────────────────┼──────────────────────────────────┐       │
│  │              Core Modules                              │       │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐      │       │
│  │  │AWS Scanner │  │ AWS Ops    │  │  Agent     │      │       │
│  │  │(13 services│  │(6 ops)    │  │(Bedrock)   │      │       │
│  │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘      │       │
│  │        │               │               │              │       │
│  │  ┌─────┴──────┐  ┌─────┴──────┐  ┌─────┴──────┐      │       │
│  │  │ Knowledge  │  │   SOP      │  │   RCA      │      │       │
│  │  │  System    │  │  System    │  │  Engine    │      │       │
│  │  └─────┬──────┘  └────────────┘  └────────────┘      │       │
│  │        │                                               │       │
│  │  ┌─────┴──────────────────────────────────────┐       │       │
│  │  │  Vector Search (S3 + OpenSearch + Bedrock)  │       │       │
│  │  └────────────────────────────────────────────┘       │       │
│  └────────────────────────────────────────────────────────┘       │
│                                                                   │
├──────────────────────────────────────────────────────────────────┤
│                    AWS Services Layer                              │
│  ┌──────────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌──────────┐        │
│  │   EC2    │ │  RDS │ │Lambda│ │DynamoDB│ │  ECS     │        │
│  │   ELB    │ │  S3  │ │ VPC  │ │Route53 │ │ElastiC.  │        │
│  │CloudWatch│ │Bedrock│ │OpenS.│ │  IAM   │ │   EKS   │        │
│  └──────────┘ └──────┘ └──────┘ └────────┘ └──────────┘        │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 模块清单

| 模块 | 文件 | 功能 |
|------|------|------|
| API Server | `api_server.py` | FastAPI 主服务，Chat + REST API |
| AWS Scanner | `src/aws_scanner.py` | 13 服务资源扫描 |
| AWS Ops | `src/aws_ops.py` | EC2/RDS/Lambda 操作 |
| Knowledge System | `src/operations_knowledge.py` | 知识库管理 |
| SOP System | `src/sop_system.py` | SOP 定义、推荐、执行 |
| Vector Search | `src/vector_search.py` | OpenSearch + Bedrock Embeddings |
| S3 Knowledge Base | `src/s3_knowledge_base.py` | S3 知识存储 |
| RCA Engine | `src/rca/engine.py` | 根因分析 |
| Multi-Agent Voting | `src/multi_agent_voting.py` | 多Agent投票决策 |
| Notifications | `src/notifications.py` | Slack 告警通知 |
| ACI Interface | `src/aci/interface.py` | Agent-Cloud 接口 |
| kubectl Wrapper | `src/kubectl_wrapper.py` | K8s 操作 |
| Bedrock Agent | `src/bedrock_agent.py` | LLM 集成 |
| Intent Classifier | `src/intent_classifier.py` | 意图识别 |

### 2.3 支持的 AWS 服务 (13个)

| 服务 | Scanner | Operations | Health Check |
|------|---------|------------|--------------|
| EC2 | ✅ | ✅ start/stop/reboot | ✅ |
| RDS | ✅ | ✅ reboot/failover | ✅ |
| Lambda | ✅ | ✅ invoke | ✅ |
| S3 | ✅ | ❌ | ✅ |
| VPC | ✅ | ❌ | ✅ |
| ELB | ✅ | ❌ | ✅ |
| Route53 | ✅ | ❌ | ✅ |
| DynamoDB | ✅ | ❌ | ✅ |
| ECS | ✅ | ❌ | ✅ |
| ElastiCache | ✅ | ❌ | ✅ |
| EKS | ✅ | ❌ | ✅ |
| CloudWatch | ✅ | ❌ | ✅ |
| IAM | ✅ | ❌ | ❌ |

---

## 3. 数据流架构

### 3.1 闭环学习系统

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Collection  │───▶│   Pattern   │───▶│   Storage   │
│  (Scanner)   │    │  (Learner)  │    │ S3+OpenSearch│
└─────────────┘    └─────────────┘    └──────┬──────┘
                                              │
      ┌───────────────────────────────────────┘
      ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Detect    │───▶│    RCA      │───▶│   Action    │
│  (Anomaly)  │    │  (Analysis) │    │  (SOP/Ops)  │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                              │
      ┌───────────────────────────────────────┘
      ▼
┌─────────────┐
│  Feedback   │──── (回到 Pattern Learner)
│  (Learn)    │
└─────────────┘
```

### 3.2 Chat 命令处理流程

```
User Input → Intent Classifier → Chat Handler → AWS API / Knowledge Base → Response
                                      │
                                      ├── AWS Scanner (scan/health)
                                      ├── AWS Ops (start/stop/reboot)
                                      ├── Knowledge System (kb search/stats)
                                      ├── SOP System (sop list/show/run)
                                      ├── Vector Search (kb semantic)
                                      └── Bedrock Agent (complex queries)
```

---

## 4. API 端点清单

### 4.1 核心 API

| Method | Endpoint | 功能 |
|--------|----------|------|
| POST | `/api/chat` | Chat 交互 |
| GET | `/health` | 健康检查 |

### 4.2 AWS 资源 API

| Method | Endpoint | 功能 |
|--------|----------|------|
| GET | `/api/cluster/info` | 集群信息 |
| GET | `/api/cluster/health` | 集群健康 |
| GET | `/api/pods` | Pod 列表 |
| GET | `/api/nodes` | Node 列表 |
| GET | `/api/anomalies` | 异常检测 |

### 4.3 SOP API

| Method | Endpoint | 功能 |
|--------|----------|------|
| GET | `/api/sop/list` | SOP 列表 |
| GET | `/api/sop/{sop_id}` | SOP 详情 |
| POST | `/api/sop/suggest` | SOP 推荐 |
| POST | `/api/sop/execute` | 执行 SOP |
| GET | `/api/sop/execution/{id}` | 执行状态 |

### 4.4 向量搜索 API

| Method | Endpoint | 功能 |
|--------|----------|------|
| GET | `/api/vector/stats` | 索引统计 |
| POST | `/api/vector/index/create` | 创建索引 |
| POST | `/api/vector/index` | 索引文档 |
| POST | `/api/vector/search` | 语义搜索 |
| POST | `/api/vector/hybrid-search` | 混合搜索 |

---

## 5. Chat 命令参考

### 5.1 资源查看

```
ec2           → EC2 实例列表
rds           → RDS 实例列表
lambda        → Lambda 函数列表
s3            → S3 桶列表
vpc           → VPC 列表
elb           → 负载均衡器列表
dynamodb      → DynamoDB 表列表
ecs           → ECS 服务列表
elasticache   → ElastiCache 集群列表
scan          → 全资源扫描
```

### 5.2 健康检查

```
health        → 全服务健康检查
ec2 health    → EC2 健康检查
rds health    → RDS 健康检查
lambda health → Lambda 健康检查
elb health    → ELB 健康检查
vpc health    → VPC 健康检查
anomaly       → 异常检测
```

### 5.3 操作命令

```
ec2 start <id>    → 启动 EC2
ec2 stop <id>     → 停止 EC2
ec2 reboot <id>   → 重启 EC2
ec2 metrics <id>  → EC2 指标
rds reboot <id>   → 重启 RDS
rds failover <id> → RDS 故障转移
lambda invoke <fn> → 调用 Lambda
```

### 5.4 知识库 & SOP

```
kb stats           → 知识库统计
kb search <query>  → 知识搜索
kb semantic <query> → 语义搜索
kb index           → 创建向量索引
sop list           → SOP 列表
sop show <id>      → SOP 详情
sop suggest <kw>   → SOP 推荐
sop run <id>       → 执行 SOP
```

---

## 6. 部署架构

### 6.1 当前部署

```
┌────────────────────────────────────────┐
│  EC2: mbot-sg-1 (m6i.xlarge)          │
│  ├── Backend: FastAPI (port 8000)      │
│  ├── Frontend: React (port 3000)       │
│  └── Region: ap-southeast-1           │
│                                        │
│  OpenSearch: os2                       │
│  ├── 3x r7g.large.search              │
│  └── Version: 2.17                     │
│                                        │
│  S3: agentic-aiops-knowledge-base      │
│  Bedrock: Claude + Titan Embeddings    │
│  IAM Role: iam-mbot-role              │
└────────────────────────────────────────┘
```

### 6.2 Git 仓库

```
Repository: ~/agentic-aiops-mvp
Branch: v2-agent-first
Commits: 30+

目录结构:
├── api_server.py          # FastAPI 主服务
├── dashboard/             # React 前端
│   └── src/
│       ├── pages/         # 页面组件
│       │   ├── AgentChat.jsx
│       │   ├── ScanConfig.jsx
│       │   ├── Metrics.jsx
│       │   └── ...
│       └── components/    # 通用组件
│           ├── ChatPanel.jsx
│           ├── DynamicDashboard.jsx
│           └── ...
├── src/                   # Python 模块
│   ├── aws_scanner.py
│   ├── aws_ops.py
│   ├── operations_knowledge.py
│   ├── sop_system.py
│   ├── vector_search.py
│   ├── aci/               # Agent-Cloud Interface
│   ├── rca/               # Root Cause Analysis
│   ├── runbook/           # Runbook 执行
│   ├── plugins/           # 服务插件
│   └── ...
├── docs/                  # 文档
│   ├── designs/
│   ├── analysis/
│   └── tests/
└── requirements.txt
```

---

## 7. 开发历史 (Sprint 记录)

### Sprint P0: 基础功能 (3 commits)
- 前端 Scan 真实数据展示
- 网络服务 (VPC/ELB/Route53)
- EC2 操作 + Help 命令

### Sprint P1: 服务扩展 (8 commits)
- DynamoDB + ECS 支持
- ElastiCache 支持
- RDS + Lambda 操作
- Slack 通知系统
- 健康检查优化

### Sprint P2: 知识系统 (6+ commits)
- Knowledge Base Enhancement
- SOP 系统 (3 内置 SOP)
- Vector Search (S3 + OpenSearch)
- OpenSearch 认证修复

### 文档 & 分析
- AWS MCP 集成设计
- Knowledge System 设计
- 故障测试计划
- 生产环境分析

---

## 8. 已知限制

| 限制 | 描述 | 优先级 |
|------|------|--------|
| 单账户 | 仅支持一个 AWS 账户 | P0 |
| 无审批流程 | 敏感操作直接执行 | P0 |
| 单点部署 | 无 HA/灾备 | P1 |
| OpenSearch 权限 | 需配置 Fine-Grained Access | P1 |
| 无 RBAC | 所有用户同权限 | P2 |
| 无回滚 | 操作不可逆 | P2 |

---

*文档结束*
