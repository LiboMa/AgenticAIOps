# AgenticAIOps 开发思路文档
**Author:** Team (Worker1, Worker2, Myboat)  
**Date:** 2026-02-01  
**Version:** 1.0

## 1. 核心开发理念

### 1.1 Ma Ronnie 的开发要求

1. **设计投票流程（至少3轮）**
   - 每个功能模块必须经过设计评审
   - 多人提案、互相评分
   - 选出最高分方案再开发

2. **质量优先**
   - 不要加速，保证质量
   - 务必保证可插拔功能
   - 完整可用的代码

3. **文档记录**
   - `docs/designs/` - 设计方案
   - `docs/reviews/` - 投票记录
   - `docs/tests/` - 测试报告

### 1.2 评分标准

| 维度 | 权重 | 说明 |
|------|------|------|
| 可扩展性 | 25% | 新功能易于添加 |
| 代码质量 | 25% | 清晰、规范、可读 |
| 用户体验 | 20% | 界面友好、操作简单 |
| 性能 | 15% | 响应快、资源占用低 |
| 可维护性 | 15% | 易于理解和修改 |

## 2. Plugin 架构设计

### 2.1 第一轮评审结果

| 方案 | 提出者 | Worker1评分 | Worker2评分 | 平均分 |
|------|--------|-------------|-------------|--------|
| Registry模式 | Worker1 | 7.65 (自评) | 7.8 | 7.73 |
| 微内核+EventBus | Worker2 | 7.55 | 8.0 (自评) | 7.78 |

### 2.2 融合方案（推荐）

```
Phase 1 (MVP): Worker1 简单 Registry 模式
  - PluginBase 抽象基类
  - PluginRegistry 单例管理
  - 简单直接，快速实现

Phase 2: 添加 Manifest 配置
  - YAML 声明式配置
  - 配置驱动插件加载

Phase 3: EventBus 支持
  - 插件间松耦合通信
  - 支持热插拔
```

### 2.3 核心接口

```python
class PluginBase(ABC):
    PLUGIN_TYPE: str
    PLUGIN_NAME: str
    PLUGIN_DESCRIPTION: str
    PLUGIN_ICON: str
    
    @abstractmethod
    def initialize(self) -> bool: ...
    
    @abstractmethod
    def health_check(self) -> Dict: ...
    
    @abstractmethod
    def get_tools(self) -> List[Callable]: ...
    
    @abstractmethod
    def get_resources(self) -> List[Dict]: ...
    
    @abstractmethod
    def get_status_summary(self) -> Dict: ...
```

## 3. 多服务支持

### 3.1 已实现的插件

| 插件 | 类型 | 功能 |
|------|------|------|
| ☸️ EKSPlugin | eks | 多EKS集群管理 |
| 🖥️ EC2Plugin | ec2 | EC2实例监控 |
| λ LambdaPlugin | lambda | Lambda函数管理 |
| 🖧 HPCPlugin | hpc | ParallelCluster/Slurm |

### 3.2 可插拔设计

```python
# 添加新插件只需：
# 1. 创建插件类继承 PluginBase
class NewServicePlugin(PluginBase):
    PLUGIN_TYPE = "newservice"
    ...

# 2. 注册插件类
PluginRegistry.register_plugin_class(NewServicePlugin)

# 3. 通过 API 或启动时创建实例
PluginRegistry.create_plugin(config)
```

## 4. 开发流程

```
┌─────────────────────────────────────────────────────────────┐
│                    开发流程                                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  设计阶段                                                     │
│  ┌─────────┐   ┌─────────┐   ┌─────────┐                    │
│  │ 提案 1  │──▶│ 提案 2  │──▶│ 提案 3  │                    │
│  └─────────┘   └─────────┘   └─────────┘                    │
│       │             │             │                          │
│       └─────────────┴─────────────┘                          │
│                     │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │ 互相评分   │                                   │
│              │ (3轮投票)  │                                   │
│              └───────────┘                                   │
│                     │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │ 最高分方案 │                                   │
│              └───────────┘                                   │
│                     │                                        │
│  开发阶段            │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │   开发     │                                   │
│              └───────────┘                                   │
│                     │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │   测试     │                                   │
│              └───────────┘                                   │
│                     │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │   文档     │                                   │
│              └───────────┘                                   │
│                     │                                        │
│                     ▼                                        │
│              ┌───────────┐                                   │
│              │   验收     │                                   │
│              └───────────┘                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 5. 技术栈

- **Agent**: Strands SDK + AWS Bedrock
- **Backend**: FastAPI + Python 3.11
- **Frontend**: React + Vite + MUI
- **RAG**: Bedrock Knowledge Base + OpenSearch Serverless
- **Infrastructure**: EKS, EC2, Lambda, S3

## 6. 目录结构

```
agentic-aiops-mvp/
├── src/
│   ├── plugins/           # 插件系统
│   │   ├── base.py        # 基类和Registry
│   │   ├── eks_plugin.py  # EKS插件
│   │   ├── ec2_plugin.py  # EC2插件
│   │   ├── lambda_plugin.py # Lambda插件
│   │   └── hpc_plugin.py  # HPC插件
│   ├── config.py          # 配置
│   └── ...
├── dashboard/             # React前端
├── docs/
│   ├── designs/           # 设计文档
│   ├── reviews/           # 评审记录
│   └── tests/             # 测试报告
├── iac/                   # IaC配置
└── eks-patterns/          # RAG知识库
```

## 7. 下一步计划

1. **完成设计投票** - 确定最终Plugin架构
2. **优化前端UI** - Plugin Manager完善
3. **添加更多插件** - 按需扩展
4. **完善文档** - 用户指南、API文档
5. **测试覆盖** - 单元测试、集成测试
