# PagerDuty Style Frontend Design Spec v2

**Version**: 2.0  
**Author**: Architect  
**Date**: 2026-02-03  
**Change**: From Ant Design Pro sidebar to PagerDuty style  
**Language**: English only (no Chinese in UI)

---

## 1. 设计方向变更

| 特性 | v1 (Ant Design Pro) | v2 (PagerDuty 风格) |
|------|---------------------|---------------------|
| 导航 | 侧边栏 | **顶部导航栏** |
| 主题 | 深色 | **浅色为主** |
| 主色 | 企业蓝 #1890ff | **PagerDuty 绿 #06ac38** |
| 布局 | 卡片网格 | **列表 + 时间线** |
| 焦点 | Dashboard 概览 | **Incident 列表** |

---

## 2. 配色方案 (PagerDuty 风格)

```css
/* 主色调 - PagerDuty 绿 */
--color-primary: #06ac38;        /* 主色 */
--color-primary-hover: #0bc445;  /* 悬停 */
--color-primary-active: #059030; /* 点击 */

/* 背景色 - 浅色主题 */
--color-bg-layout: #f8fafc;      /* 页面背景 */
--color-bg-container: #ffffff;   /* 卡片背景 */
--color-bg-header: #1e293b;      /* 顶部导航 (深色) */

/* 状态色 - 高对比 */
--color-critical: #e11d48;       /* 严重 - 红色 */
--color-high: #f97316;           /* 高 - 橙色 */
--color-warning: #eab308;        /* 警告 - 黄色 */
--color-low: #22c55e;            /* 低/正常 - 绿色 */
--color-info: #3b82f6;           /* 信息 - 蓝色 */

/* 文字色 */
--color-text-primary: #1e293b;   /* 主要文字 (深灰) */
--color-text-secondary: #64748b; /* 次要文字 */
--color-text-inverse: #ffffff;   /* 反色文字 (导航栏) */

/* 边框 */
--color-border: #e2e8f0;         /* 边框色 */
```

---

## 3. 布局结构 (PagerDuty 风格)

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo]   Incidents  Services  Analytics  On-Call    [User] [⚙] │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  All Incidents (5 open)                      [Filters ▼] │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │ 🔴 CRITICAL | #INC-001                                  │   │
│   │    Pod OOMKilled in production                          │   │
│   │    Triggered 5m ago • stress-test • Assigned: Auto      │   │
│   │                                              [Ack] [Res] │   │
│   ├─────────────────────────────────────────────────────────┤   │
│   │ 🟡 HIGH | #INC-002                                      │   │
│   │    High CPU usage on web-server                         │   │
│   │    Triggered 15m ago • default • Acknowledged           │   │
│   │                                              [Esc] [Res] │   │
│   ├─────────────────────────────────────────────────────────┤   │
│   │ 🟢 RESOLVED | #INC-003                                  │   │
│   │    Memory leak detected                                 │   │
│   │    Resolved 2h ago • Duration: 45m                      │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  Service Health                                          │   │
│   │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │   │
│   │  │ 🟢 API │ │ 🟢 Web │ │ 🟡 DB  │ │ 🟢 Cache│           │   │
│   │  │ 100%   │ │ 100%   │ │ 95%    │ │ 100%   │           │   │
│   │  └────────┘ └────────┘ └────────┘ └────────┘           │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 核心组件

### 4.1 顶部导航栏 (Header)

```tsx
// 高度: 64px
// 背景: #1e293b (深灰)
// 文字: 白色

<Header>
  <Logo />
  <NavMenu>
    <NavItem active>Incidents</NavItem>
    <NavItem>Services</NavItem>
    <NavItem>Analytics</NavItem>
    <NavItem>On-Call</NavItem>
  </NavMenu>
  <UserMenu />
</Header>
```

### 4.2 Incident 卡片

```tsx
interface IncidentCard {
  id: string;           // INC-001
  title: string;
  severity: 'critical' | 'high' | 'warning' | 'low';
  status: 'triggered' | 'acknowledged' | 'resolved';
  triggeredAt: Date;
  namespace: string;
  assignee?: string;
  duration?: string;    // 已解决时显示
}

// 左边框颜色表示严重程度
// critical: 红色 4px 左边框
// high: 橙色
// warning: 黄色
// low/resolved: 绿色
```

### 4.3 Service Health 卡片

```tsx
interface ServiceCard {
  name: string;
  status: 'healthy' | 'degraded' | 'down';
  uptime: string;       // "100%"
  incidents: number;    // 当前 incident 数
}

// 小型正方形卡片
// 状态用圆点颜色表示
```

### 4.4 时间线视图 (Incident Timeline)

```tsx
// 显示 incident 生命周期
Timeline:
  ├── 09:00 🔴 Triggered - OOMKilled detected
  ├── 09:02 📋 Acknowledged by Auto-remediation
  ├── 09:03 🔧 Action: Increased memory limit
  └── 09:05 ✅ Resolved - Pod healthy
```

---

## 5. 页面结构

| 页面 | 路由 | 功能 |
|------|------|------|
| **Incidents** | `/` | Incident 列表 (主页) |
| Incident Detail | `/incidents/:id` | 详情 + 时间线 |
| **Services** | `/services` | 服务健康状态 |
| **Analytics** | `/analytics` | 统计图表 |
| **On-Call** | `/on-call` | 值班管理 |
| Settings | `/settings` | 系统设置 |

---

## 6. 技术实现

```yaml
# 保持 Ant Design 组件库，但调整样式

组件映射:
  Header: Ant Design Menu (horizontal)
  IncidentCard: Ant Design Card + 自定义样式
  ServiceHealth: Ant Design Statistic + Grid
  Timeline: Ant Design Timeline
  Table: Ant Design Table (列表视图)

配置:
  theme:
    token:
      colorPrimary: '#06ac38'
      colorBgLayout: '#f8fafc'
      colorBgContainer: '#ffffff'
```

---

## 7. 关键差异

```
PagerDuty 设计哲学:
├── "Incident-first" - 事件是核心
├── "Glanceable" - 一眼看清状态
├── "Action-oriented" - 快速操作
└── "Timeline-based" - 时间线叙事

实现要点:
├── 浅色背景 + 深色导航
├── 状态颜色高对比
├── 列表视图 > 卡片网格
├── 大量留白，信息清晰
└── 快速操作按钮 (Ack/Resolve/Escalate)
```

---

**设计状态**: 📝 待评审  
**下一步**: @Developer 按此规范重构
