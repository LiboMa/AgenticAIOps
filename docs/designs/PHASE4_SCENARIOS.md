# Phase 4: 实际场景集成设计文档

**版本**: 1.0  
**作者**: Architect  
**日期**: 2026-02-02  
**参考**: AIOpsLab Framework, mABC Paper

---

## 1. 概述

### 1.1 目标

验证 AgenticAIOps 在真实 EKS 集群上的故障检测、定位和修复能力。

### 1.2 测试环境

```
集群: testing-cluster
区域: ap-southeast-1
监控: Prometheus + Grafana
MCP: awslabs.eks-mcp-server (16 工具)
```

---

## 2. 场景设计 (基于 AIOpsLab P = ⟨T, C, S⟩)

### 2.1 场景 1: Pod OOM (内存溢出)

```
┌─────────────────────────────────────────────────────────────┐
│  场景 1: Pod OOM Killed                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Problem Definition:                                         │
│  P = ⟨T, C, S⟩                                               │
│                                                              │
│  T (Task): Detection + Localization + Analysis               │
│  C (Context):                                                │
│    - Namespace: stress-test                                  │
│    - Pod: memory-stress                                      │
│    - 症状: Pod 频繁重启, OOMKilled                           │
│  S (Solution): 识别 OOM 根因, 建议增加内存限制               │
│                                                              │
│  故障注入:                                                    │
│  kubectl run memory-stress --image=polinux/stress \          │
│    --limits="memory=64Mi" \                                  │
│    -- stress --vm 1 --vm-bytes 128M                          │
│                                                              │
│  预期 Agent 响应:                                            │
│  1. 检测: get_events() → OOMKilled 事件                      │
│  2. 定位: get_pod_logs() → 内存分配失败                      │
│  3. 分析: get_metrics() → 内存使用超限                       │
│  4. 建议: 增加 memory limit 或优化应用                       │
│                                                              │
│  评估指标:                                                    │
│  • TTD (Time-to-Detect): < 30s                              │
│  • TTL (Time-to-Localize): < 60s                            │
│  • Accuracy: 正确识别 OOM                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**故障注入脚本:**

```yaml
# manifests/stress-test/memory-stress.yaml
apiVersion: v1
kind: Pod
metadata:
  name: memory-stress
  namespace: stress-test
spec:
  containers:
  - name: stress
    image: polinux/stress
    resources:
      limits:
        memory: "64Mi"
      requests:
        memory: "32Mi"
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "128M", "--vm-hang", "1"]
  restartPolicy: Always
```

---

### 2.2 场景 2: 高 CPU 使用率

```
┌─────────────────────────────────────────────────────────────┐
│  场景 2: CPU Throttling                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Problem Definition:                                         │
│  P = ⟨T, C, S⟩                                               │
│                                                              │
│  T (Task): Detection + Localization + Analysis               │
│  C (Context):                                                │
│    - Namespace: stress-test                                  │
│    - Pod: cpu-stress                                         │
│    - 症状: 响应延迟, CPU Throttling                          │
│  S (Solution): 识别 CPU 限流, 建议调整 CPU limits            │
│                                                              │
│  故障注入:                                                    │
│  kubectl run cpu-stress --image=polinux/stress \             │
│    --limits="cpu=100m" \                                     │
│    -- stress --cpu 2                                         │
│                                                              │
│  预期 Agent 响应:                                            │
│  1. 检测: get_metrics() → CPU 使用率 100%                    │
│  2. 定位: Prometheus → container_cpu_cfs_throttled           │
│  3. 分析: CPU limit 过低导致限流                             │
│  4. 建议: 增加 CPU limit 或优化代码                          │
│                                                              │
│  评估指标:                                                    │
│  • TTD: < 60s                                                │
│  • TTL: < 120s                                               │
│  • Accuracy: 正确识别 CPU throttling                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**故障注入脚本:**

```yaml
# manifests/stress-test/cpu-stress.yaml
apiVersion: v1
kind: Pod
metadata:
  name: cpu-stress
  namespace: stress-test
spec:
  containers:
  - name: stress
    image: polinux/stress
    resources:
      limits:
        cpu: "100m"
      requests:
        cpu: "50m"
    command: ["stress"]
    args: ["--cpu", "2"]
  restartPolicy: Always
```

---

### 2.3 场景 3: ImagePullBackOff

```
┌─────────────────────────────────────────────────────────────┐
│  场景 3: ImagePullBackOff                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Problem Definition:                                         │
│  P = ⟨T, C, S⟩                                               │
│                                                              │
│  T (Task): Detection + Analysis                              │
│  C (Context):                                                │
│    - Namespace: stress-test                                  │
│    - Pod: bad-image                                          │
│    - 症状: Pod Pending, ImagePullBackOff                     │
│  S (Solution): 识别镜像不存在, 建议检查镜像名称              │
│                                                              │
│  故障注入:                                                    │
│  kubectl run bad-image --image=nonexistent/image:v999        │
│                                                              │
│  预期 Agent 响应:                                            │
│  1. 检测: get_events() → Failed to pull image               │
│  2. 分析: 镜像仓库/标签不存在                                │
│  3. 建议: 检查镜像名称, 确认仓库可访问                       │
│                                                              │
│  评估指标:                                                    │
│  • TTD: < 30s                                                │
│  • Accuracy: 正确识别 ImagePullBackOff                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

### 2.4 场景 4: 服务连接失败 (网络问题)

```
┌─────────────────────────────────────────────────────────────┐
│  场景 4: Service Unreachable                                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Problem Definition:                                         │
│  P = ⟨T, C, S⟩                                               │
│                                                              │
│  T (Task): Localization + Analysis                           │
│  C (Context):                                                │
│    - Namespace: stress-test                                  │
│    - Service: backend-svc                                    │
│    - 症状: Connection refused, 服务不可达                    │
│  S (Solution): 识别 Service 配置错误                         │
│                                                              │
│  故障注入:                                                    │
│  # 创建 Service 指向不存在的 Pod selector                    │
│  kubectl create svc clusterip backend-svc --tcp=80:80        │
│  # 但没有匹配的 Pod                                          │
│                                                              │
│  预期 Agent 响应:                                            │
│  1. 检测: kubectl get endpoints → 空                         │
│  2. 分析: Service selector 无匹配 Pod                        │
│  3. 建议: 检查 selector 或部署匹配的 Pod                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Agent 响应流程

### 3.1 诊断流程

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent 诊断流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  用户输入: "stress-test namespace 的 Pod 有问题"             │
│                                                              │
│      │                                                       │
│      ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Step 1: 数据收集 (ACI Telemetry)                        ││
│  │                                                         ││
│  │ • get_events(namespace="stress-test")                   ││
│  │ • get_logs(namespace="stress-test")                     ││
│  │ • get_metrics(namespace="stress-test")                  ││
│  │ • kubectl(["get", "pods", "-n", "stress-test"])        ││
│  └─────────────────────────────────────────────────────────┘│
│      │                                                       │
│      ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Step 2: 多 Agent 分析 (Voting)                          ││
│  │                                                         ││
│  │ • Architect: 分析架构层面问题                           ││
│  │ • Developer: 分析代码/配置问题                          ││
│  │ • Tester: 验证症状和复现                                ││
│  │                                                         ││
│  │ → 加权投票 → 最终诊断                                   ││
│  └─────────────────────────────────────────────────────────┘│
│      │                                                       │
│      ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Step 3: 输出结果                                        ││
│  │                                                         ││
│  │ • 诊断结论 (OOM/CPU/ImagePull/Network)                  ││
│  │ • 置信度 (共识度)                                       ││
│  │ • 修复建议                                              ││
│  │ • 相关证据 (日志/指标/事件)                             ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 端到端测试流程

```python
# tests/e2e/test_scenarios.py

import pytest
from src.aci import AgentCloudInterface
from src.voting import MultiAgentVoting, TaskType

class TestOOMScenario:
    """场景 1: OOM 测试"""
    
    @pytest.fixture
    def setup_oom_pod(self, kubectl):
        """注入 OOM 故障"""
        kubectl.apply("manifests/stress-test/memory-stress.yaml")
        yield
        kubectl.delete("manifests/stress-test/memory-stress.yaml")
    
    def test_detect_oom(self, setup_oom_pod, aci):
        """测试 OOM 检测"""
        # 等待 Pod 崩溃
        time.sleep(30)
        
        # 获取事件
        events = aci.get_events(namespace="stress-test")
        
        # 验证检测到 OOMKilled
        assert any("OOMKilled" in str(e) for e in events.data)
    
    def test_diagnose_oom(self, setup_oom_pod, voting):
        """测试 OOM 诊断"""
        # 模拟 Agent 响应
        responses = {
            "architect": "根据事件日志，Pod 因 OOMKilled 重启",
            "developer": "内存限制 64Mi，应用申请 128M，超出限制",
            "tester": "复现了 OOM，容器内存不足"
        }
        
        result = voting.vote(
            task_type=TaskType.ANALYSIS,
            query="memory-stress Pod 问题诊断",
            agent_responses=responses
        )
        
        assert result.final_answer == "oom"
        assert result.consensus == True
```

---

## 4. 验收标准

### 4.1 功能验收

| 场景 | 检测 | 定位 | 分析 | 建议 |
|------|------|------|------|------|
| OOM | ✓ 识别 OOMKilled | ✓ 定位 Pod | ✓ 内存超限 | ✓ 增加 limit |
| CPU | ✓ 识别 Throttling | ✓ 定位 Pod | ✓ CPU 限流 | ✓ 调整 limit |
| Image | ✓ 识别 ImagePull | ✓ 定位 Pod | ✓ 镜像不存在 | ✓ 检查镜像 |
| Network | ✓ 识别连接失败 | ✓ 定位 Service | ✓ Endpoint 空 | ✓ 检查 selector |

### 4.2 性能验收

| 指标 | 目标 | 说明 |
|------|------|------|
| TTD (Time-to-Detect) | < 30s | 检测到异常的时间 |
| TTL (Time-to-Localize) | < 60s | 定位根因的时间 |
| Accuracy | > 90% | 诊断准确率 |
| Consensus Rate | > 80% | 多 Agent 共识率 |

### 4.3 质量验收

```
✓ 所有场景端到端测试通过
✓ 诊断结果与预期一致
✓ 无误报 (False Positive < 5%)
✓ 无漏报 (False Negative < 5%)
```

---

## 5. 实施计划

### 5.1 阶段划分

| 阶段 | 内容 | 负责人 | 预计时间 |
|------|------|--------|----------|
| **4.1** | 场景设计 (本文档) | Architect | ✅ 完成 |
| **4.2** | 故障注入脚本 | Developer | 0.5 天 |
| **4.3** | 端到端测试 | Developer | 1 天 |
| **4.4** | 验证测试 | Tester | 0.5 天 |
| **4.5** | 评审 | Reviewer | 0.5 天 |

### 5.2 文件清单

```
新增文件:
├── manifests/stress-test/
│   ├── namespace.yaml           # stress-test namespace
│   ├── memory-stress.yaml       # OOM 场景
│   ├── cpu-stress.yaml          # CPU 场景
│   ├── bad-image.yaml           # ImagePull 场景
│   └── broken-service.yaml      # Network 场景
├── tests/e2e/
│   ├── __init__.py
│   ├── conftest.py              # E2E fixtures
│   ├── test_oom_scenario.py
│   ├── test_cpu_scenario.py
│   ├── test_image_scenario.py
│   └── test_network_scenario.py
└── docs/designs/
    └── PHASE4_SCENARIOS.md      # 本文件
```

---

## 6. 评审检查点

- [ ] 场景覆盖是否全面
- [ ] 故障注入是否安全可控
- [ ] 验收标准是否合理
- [ ] 与现有系统集成是否可行

---

**设计状态**: 📝 待评审  
**下一步**: 提交给 @Reviewer 评审，通过后 @Developer 开始实现故障注入脚本
